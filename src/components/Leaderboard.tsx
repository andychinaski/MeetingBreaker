import type { GameModeId } from '../game/types/mode';
import type { LeaderboardEntry } from '../services/storageService';
import styles from './MainMenu.module.css';
export function Leaderboard({ entries, onClear, onBack }: { entries: LeaderboardEntry[]; onClear: () => void; onBack: () => void }) { const tabs: GameModeId[] = ['campaign', 'relax', 'hard']; const ranked = (mode: GameModeId) => entries.filter((entry) => entry.mode === mode).sort((a, b) => mode === 'relax' ? b.freedMinutes - a.freedMinutes || b.durationSeconds - a.durationSeconds : b.score - a.score).slice(0, 10); return <section className={styles.menuCard}><div><p className={styles.kicker}>Только на этом устройстве</p><h2>Локальная таблица лидеров</h2></div>{tabs.map((mode) => <div key={mode}><h3>{mode === 'campaign' ? 'Прохождение' : mode === 'relax' ? 'Relax' : 'Hard'}</h3><ol>{ranked(mode).map((entry) => <li key={entry.id}>{entry.playerName} — {entry.score.toLocaleString('ru-RU')} · {entry.freedMinutes} мин · {entry.durationSeconds} сек</li>)}</ol></div>)}{entries.length === 0 && <p>Результатов пока нет.</p>}<div className={styles.actions}><button className={styles.settingsButton} onClick={onBack}>Назад</button><button className={styles.settingsButton} onClick={onClear}>Очистить результаты</button></div></section>; }
